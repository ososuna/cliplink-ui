---
const token = Astro.cookies.get("access_token")?.value;

// const getUrlsUseCase = makeGetUrls(token);
// const result = await getUrlsUseCase.execute(1, 1, '');
// const urlsCount = result.ok ? result.value.total : 0;
const urlsCount = 0;
---

<p class="flex items-center">
  <span class="text-2xl font-bold mr-2">{urlsCount}</span>
  URLs shortened
</p>
